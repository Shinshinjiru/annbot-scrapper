spring:
  application:
    name: annbot-scrapper

  cloud:
    config:
      uri: ${CONFIGSERVER_HOST}
      username: root
      password: s3cr3t

  rabbitmq:
    host: ${RABBITMQ_HOST}
    port: ${RABBITMQ_PORT}
    virtual-host: ${RABBITMQ_USERNAME}
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASSWORD}
